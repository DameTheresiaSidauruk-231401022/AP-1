#include <iostream>  
#include "function_header.h"  // Menyertakan file header yang berisi deklarasi fungsi
#include "preprocessor.h"  // Menyertakan file header yang berisi definisi konstanta

u;  // Deklarasi variabel

// Function
int tambah(int a, int b) {  // Mendefinisikan fungsi tambah yang mengembalikan hasil penjumlahan dua bilangan
    return a + b;
}

int kurang(int a, int b) {  // Mendefinisikan fungsi kurang yang mengembalikan hasil pengurangan dua bilangan
    return a - b;
}

// Procedure
void garis() {  // Mendefinisikan prosedur garis untuk menampilkan garis
    cout << "==========" << endl;
}

void bintang() {  // Mendefinisikan prosedur bintang untuk menampilkan bintang
    cout << "*********" << endl;
}

begin  // Memulai blok program utama
    s;  // system("cls")
    int n;  // Mendeklarasikan variabel n untuk menyimpan nilai yang akan dimasukkan pengguna
    garis();  // Memanggil prosedur garis untuk menampilkan garis
    o l tambah(2, 3) l e;  // Memanggil fungsi tambah dengan argumen 2 dan 3, dan menampilkan hasilnya
    o l kurang(2, 3) l e;  // Memanggil fungsi kurang dengan argumen 2 dan 3, dan menampilkan hasilnya
    o l kali(2, 3) l e;  // Memanggil fungsi kali dengan argumen 2 dan 3, namun tidak terdefinisi dalam program
    o l bagi(2, 3) l e;  // Memanggil fungsi bagi dengan argumen 2 dan 3, namun tidak terdefinisi dalam program
    bintang();  // Memanggil prosedur bintang untuk menampilkan bintang
    o l PI l e;  // Menampilkan nilai konstanta PI yang didefinisikan dalam file header
    bintang();  // Memanggil prosedur bintang untuk menampilkan bintang
    o l "Masukkan angka : ";  // Menampilkan pesan "Masukkan angka : "
    i r n;  // Membaca input angka dan menyimpannya dalam variabel n
    bintang();  // Memanggil prosedur bintang untuk menampilkan bintang
    o l n l e;  // Menampilkan nilai yang dimasukkan pengguna
end  // Mengakhiri blok program utama
